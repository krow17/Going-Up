using UnityEngine;
using System.Collections;

public class StartLvl2 : MonoBehaviour {

    public GameObject player;

	// Use this for initialization
	void Start ()
    {
        player.transform.position = new Vector3(-26.7f, 0.827f, 1.341f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
